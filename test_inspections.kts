val unusedVar = 42

val usedVar = 10
println("Used variable: $usedVar")

val broken = "unclosed string

undefinedFunction()

val numbers = listOf(1, 2, 3, 4, 5)
numbers.forEach { println(it) }

val anotherUnused = "test"

println("Inspection test completed!")

